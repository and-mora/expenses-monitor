namespace: expenses-monitor
resources:
  - manifest.yaml
  - httproute.yaml
configMapGenerator:
  - name: frontend-nginx-config
    files:
      - default.conf
generatorOptions:
  disableNameSuffixHash: true
